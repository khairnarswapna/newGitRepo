#!/bin/bash -x
isfulltime=1;
isparttime=2;
max_hours_month=50;
rateperhour=200;
no_of _workingday=10;
max_working_days=20;
totalworkinghours=0;
totalworkingDays=0;
function getworking_hour()
{
      case $1 in 
      $isfulltime)
      workinghrs=8
      ;;
       $isparttime)
      workinghrs=4
      ;;
      *)
      workinghrs=0;
      esac
      echo $workinghrs
      
}
function calculateDailywage()
{
   wage=$(($1*$rateperhour))
   echo $wage
}
while [[ $totalworinghours -lt $max_hours_month && $totalworkingdays -lt $max_working_days ]]

do 
    ((totalworkingdays++))
    

       echo total working days $to




























